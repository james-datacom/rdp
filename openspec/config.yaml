schema: spec-driven

context: |
  Tech stack: ASP.NET Core (.NET 10) API, React + Vite web app.
  Product maps: root README, web/README.md. 
  Customer rules: rdp-specifications/
  Do not implement from archived OpenSpec changes. Do not recreate a context/ spec tree.
  Story tests match rdp-specifications/, not seed data.
  UI display text: hyphen-minus, never em-dashes.

# Project context (optional)
# Add custom rules for specific artifacts.
